name: primera_sesion_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  fl_chart: ^0.68.0
  intl: ^0.19.0
  provider: ^6.1.2
  cupertino_icons: ^1.0.6
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
